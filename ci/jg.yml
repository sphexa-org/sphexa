include:
  - local: 'ci/common.yml'

variables:
  MYUENV: "prgenv-gnu/24.11:v1"

sph-build-cuda12-sm90-debug:
  stage: sphexa_build
  # needs: ['sph:base:cuda12']
  #no image: $(./scripts/cscs.sh uenv_image_find)
  image: "${MYUENV}"
  # image: prgenv-gnu/24.11:v1
  extends: [.uenv-runner-daint-gh200]
  variables:
    # PERSIST_IMAGE_NAME: ${CSCS_REGISTRY_PATH}/sphexa_cuda12_debug:${CI_COMMIT_SHORT_SHA}
    WITH_UENV_VIEW: 'default'
    # SLURM_PARTITION: 'debug'
    SLURM_TIMELIMIT: '0:1:0'
    SLURM_JOB_NUM_NODES: 1
  before_script:
    - pwd
    - git log -n1
    - git remote -v
    - git --version
  script:
    - date
# 
# sph-run-cuda12-sm90-debug:
#   stage: sphexa_run
#   needs: ['sph-build-cuda12-sm90-debug']
#   image: prgenv-gnu/24.11:v1
#   extends: [.uenv-runner-daint-gh200]
#   variables:
#     WITH_UENV_VIEW: 'default'
#     SLURM_JOB_NUM_NODES: 1
#   before_script:
#     - nvcc --version
#   script:
#     - date
